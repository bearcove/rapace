//! Placeholder for the in-process transport implementation.
//!
//! The actual code still lives in `rapace-transport-mem`. As we merge the
//! transports under one crate, this module will host the in-process transport
//! types directly so callers only depend on `rapace-transport`.

/// TODO: copy `rapace-transport-mem` implementation in here.
pub struct MemTransportNotYetMerged;
